# REST Countries v5 key (https://restcountries.com/api-keys).
# The key must list your site hostname(s) under CORS allowed origins,
# e.g. mochatrade-three.vercel.app, localhost
VITE_RESTCOUNTRIES_KEY=
