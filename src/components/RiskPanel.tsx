// TODO(P2): build the real component. Stub only, so the folder/import
// path exists and P3 has something to wire into screens/Dashboard.tsx.
import type { ReactElement } from "react";

export default function RiskPanel(): ReactElement {
  return <div className="p-4 border border-dashed border-neutral-700 rounded text-neutral-500">RiskPanel -- not built yet</div>;
}
