// TODO(P2): assemble MarketSelector, ScoreCard, RankingVsSequence,
// RiskPanel, GoGateTimeline, ComplianceChecklist and WeightControls here.
// Pull data via src/hooks/*, which wrap src/engine (see docs/SPEC.md).
import type { ReactElement } from "react";

export default function Dashboard(): ReactElement {
  return <div>Dashboard -- not built yet. See docs/SPEC.md section F for the screen map.</div>;
}
